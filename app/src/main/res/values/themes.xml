<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Vaseelaser" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="DialogHelperAnim">
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
    </style>

    <style name="BottomDialogAnim">
        <item name="android:windowEnterAnimation">@anim/progress_show</item>
        <item name="android:windowExitAnimation">@anim/progress_dismiss</item>
    </style>

    <!-- buttons ok cancel-->
    <style name="AppTheme.BottomButtonsOKCancel" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:textColor">@color/primary</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="CompatPopupAlert" parent="Theme.Vaseelaser">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <!--<item name="android:windowExitAnimation">@anim/slide_out </item>-->
    </style>
</resources>